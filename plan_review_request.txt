1. Implement the `calcular_rn` method in `backend/models/irrigacao.py`.
   - The method should accept `t_max_c`, `t_min_c`, `ea`, `rs`, `rso`, and `rns`.
   - It needs to calculate absolute temperatures in Kelvin by adding 273.16 to `t_max_c` and `t_min_c`.
   - Use the Stefan-Boltzmann constant $\sigma = 4.903 \times 10^{-9}$.
   - Calculate $R_{nl}$ using Equation 19: `R_nl = ((sigma * T_max_K**4 + sigma * T_min_K**4) / 2) * (0.34 - 0.14 * math.sqrt(ea)) * (1.35 * (rs / rso) - 0.35)`.
   - Calculate $R_n$ using Equation 20: `R_n = rns - R_nl`.
   - Ensure the output limits or edge cases like `rso=0` are handled (e.g. returning 0 if `rso <= 0` to prevent division by zero).
   - Return the calculated $R_{nl}$ and $R_n$.
2. Write unit tests for `calcular_rn`.
   - Test various inputs including the edge cases like `rso=0`.
3. Complete pre-commit steps.
   - I will ensure all verifications, tests, and formatting are correctly checked using the `pre_commit_instructions` tool.
4. Submit the change.
